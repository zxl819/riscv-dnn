# llvm编译器环境搭建v2

> [!CAUTION]
>
> 此版本增加了1.6对测试负载批量编译所需要的python环境，同时在1.5记录了一些配置编译链时出现的一些常见问题。

[TOC]

# **1. 官方 RISC-V Matrix Project 环境配置指南**

------

> 官网链接 https://github.com/riscv-stc/riscv-matrix-project

------

该项目提供了一系列支持 RISC-V 矩阵扩展指令开发的工具链组件，包括：

- **riscv-matrix-spec**：RISC-V 矩阵扩展指令提案文档
- **llvm-project**：支持矩阵扩展的 LLVM 编译工具链
- **riscv-isa-sim**：Spike 模拟器，支持矩阵扩展
- **chipyard**：基于 BOOM Core 的 SoC 设计平台，支持矩阵扩展
- **riscv-pvp-matrix**：基于 RISC-V PVP 的矩阵扩展指令验证
- **riscv-dnn**：基于 RISC-V 向量与矩阵扩展实现的小型 DNN 库

我们只需要下载 `llvm-project` 和 `riscv-gnu-toolchain` 两部分内容

## **1.1 环境准备与基本配置**

首先，确保你的虚拟机中已有 `~/opt/riscv` 目录，并配置如下环境变量：

```bash
export RISCV=~/opt/riscv
export PATH=$RISCV/bin:$PATH
```

然后克隆项目主仓库：

```bash
git clone <https://github.com/riscv-stc/riscv-matrix-project>
```

执行上述命令后，`~/opt/riscv/riscv-matrix-project` 目录会被创建，初始子目录均为空。接下来我们将手动克隆并构建所需的子模块

## **1.2 构建支持矩阵扩展的 LLVM 工具链**

该步骤构建的是 `riscv-matrix-spec v0.5` 所需的 `clang + llvm` 工具链

### **1.2.1 克隆与编译**

```bash
cd ~/opt/riscv/riscv-matrix-project
git clone --branch matrix <https://github.com/riscv-stc/llvm-project.git>

cd llvm-project
mkdir -p build && cd build
cmake -DCMAKE_INSTALL_PREFIX=$RISCV \\
      -DCMAKE_BUILD_TYPE=Release \\
      -DLLVM_OPTIMIZED_TABLEGEN=On \\
      -DLLVM_ENABLE_PROJECTS="clang;compiler-rt;lld" \\
      -DLLVM_LINK_LLVM_DYLIB=On ../llvm
```

然后开始编译（约需 30min ~ 1h，建议虚拟机为不低于 12 GB 内存、处理器 2*4 的配置）：

```bash
make -j$(nproc) && make install
```

编译时会出现一个错误：

```bash
/home/xjy/opt/riscv/riscv-matrix-project/llvm-project/llvm/include/llvm/Support/Signals.h:18:1: note: ‘uintptr_t’ is defined in header ‘<cstdint>’; did you forget to ‘#include <cstdint>’?
   17 | #include <string>
  +++ |+#include <cstdint>
   18 |
```

只要按提示补充头文件即可。

## **1.3 构建 riscv-gnu-toolchain 工具链（ELF）**

该工具链用于将 clang 编译得到的目标文件链接为裸机可执行文件，适用于 `gem5 SE` 模式等场景

### **1.3.1 所需子模块说明**

| **子模块名**     | **是否必须** | **用途说明**                                                 |
| ---------------- | ------------ | ------------------------------------------------------------ |
| `riscv-binutils` | ✅ 是         | 提供汇编器和链接器（如 `as`, `ld`），处理 `.s` 和 `.o` 文件。 |
| `riscv-gcc`      | ✅ 是         | 提供 GCC 编译器驱动，与 Clang 配合生成 ELF 工具链。          |
| `riscv-glibc`    | ❌ 否         | Linux 系统运行时库，**不适用于裸机开发**。                   |
| `riscv-gdb`      | ✅ 可选       | 用于调试裸机程序，在 gem5 中支持单步和断点。                 |
| `riscv-newlib`   | ✅ 是         | 提供裸机下的标准 C 库（如 `printf`, `malloc`）。             |
| `riscv-dejagnu`  | ❌ 可选       | 回归测试框架，可忽略，不影响工具链构建与使用。               |
| `qemu`           | ❌ 否         | QEMU 模拟器，使用 gem5 时可不构建。                          |

### **1.3.2 安装构建依赖**

```bash
sudo apt update
sudo apt-get install autoconf automake autotools-dev curl \\
  libmpc-dev libmpfr-dev libgmp-dev gawk build-essential \\
  bison flex texinfo gperf libtool patchutils bc zlib1g-dev \\
  libexpat-dev python3
```

### **1.3.3 克隆与构建**

克隆仓库（仅拉取我们所需的模块）：

```bash
cd ~/opt/riscv/riscv-matrix-project
git clone <https://github.com/riscv-collab/riscv-gnu-toolchain.git>

cd riscv-gnu-toolchain
git submodule update --init binutils
git submodule update --init gcc
git submodule update --init gdb
git submodule update --init newlib
git submodule update --init dejagnu
```

> git clone https://gitee.com/mirrors/riscv-gnu-toolchain.git

换源（未尝试）

配置我们需要下载的模块，安装到 `~/opt/riscv`，且支持 32 位和 64 位：

```bash
./configure --prefix=$HOME/opt/riscv --enable-multilib
```

开始构建（约需 1h 左右）：

```bash
make -j$(nproc)
```

### **1.3.4 构建完成验证**

通过以下指令验证一下工具链是否可用：

```bash
# 确认 gcc 工具链是否安装成功
~/opt/riscv/bin/riscv64-unknown-elf-gcc --version
# 检查 multilib 架构支持情况
~/opt/riscv/bin/riscv64-unknown-elf-gcc --print-multi-lib
```

预期输出如下：

```bash
# 确认 gcc 工具链是否安装成功
riscv64-unknown-elf-gcc (g04696df0963) 14.2.0
Copyright (C) 2024 Free Software Foundation, Inc.
This is free software; see the source for copying conditions.  There is NO
warranty; not even for MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.

# 检查 multilib 架构支持情况
.;
rv32i/ilp32;@march=rv32i@mabi=ilp32
rv32im/ilp32;@march=rv32im@mabi=ilp32
rv32iac/ilp32;@march=rv32iac@mabi=ilp32
rv32imac/ilp32;@march=rv32imac@mabi=ilp32
rv32imafc/ilp32f;@march=rv32imafc@mabi=ilp32f
rv64imac/lp64;@march=rv64imac@mabi=lp64
rv64imafdc/lp64d;@march=rv64imafdc@mabi=lp64d
```

## **1.4 工具链测试：Clang + LLVM + GNU Toolchain**

在测试目录下创建 `mset.c` 文件，并写入如下内容（用于测试自定义 RISC-V Matrix 扩展指令）：

```c
#include <stdint.h>
#include <stdio.h>

void getMtype(uint64_t *csr_data, uint64_t *reg_data) {
    __asm__ volatile("csrr %0, 0xc40" : "=r"(*csr_data));
    __asm__ volatile("mv %0, x5" : "=r"(*reg_data));
}

void getMtilem(uint64_t *csr_data, uint64_t *reg_data) {
    __asm__ volatile("csrr %0, 0xc41" : "=r"(*csr_data));
    __asm__ volatile("mv %0, x4" : "=r"(*reg_data));
}

int main() {
    uint64_t csr_init, csr_msettypei, csr_msettype, csr_msettypehi, csr_msetsew, csr_msetint4;
    uint64_t x5_init, x5_msettypei, x5_msettype, x5_msettypehi, x5_msetsew, x5_msetint4;

    getMtype(&csr_init, &x5_init);
    __asm__ volatile("msettypei x5, e16");

    getMtype(&csr_msettypei, &x5_msettypei);

    uint32_t csr_temp = 0x12345678;
    __asm__ volatile("mv x6, %0" : : "r"(csr_temp) : );
    __asm__ volatile("msettype x5, x6");

    getMtype(&csr_msettype, &x5_msettype);

    __asm__ volatile("msettypehi x5, ba");
    getMtype(&csr_msettypehi, &x5_msettypehi);

    __asm__ volatile("msetsew x5, 1");
    getMtype(&csr_msetsew, &x5_msetsew);

    __asm__ volatile("msetint4 x5, 0");
    getMtype(&csr_msetint4, &x5_msetint4);

    uint64_t mtilem_init, x4_init;
    getMtilem(&mtilem_init, &x4_init);

    uint64_t mtiem_temp = 0xA;
    __asm__ volatile("mv x6, %0" : : "r"(mtiem_temp) : );
    __asm__ volatile("msettilem x4, x6");

    uint64_t mtilem_msettilem, x4_msettilem;
    getMtilem(&mtilem_msettilem, &x4_msettilem);

    printf("Init:\\n");
    printf("  CSR[c40] = 0x%lx\\n", csr_init);
    printf("  x5       = 0x%lx\\n", x5_init);
    printf("After msettypei:\\n");
    printf("  CSR[c40] = 0x%lx\\n", csr_msettypei);
    printf("  x5       = 0x%lx\\n", x5_msettypei);
    printf("After msettype:\\n");
    printf("  CSR[c40] = 0x%lx\\n", csr_msettype);
    printf("  x5       = 0x%lx\\n", x5_msettype);
    printf("After msettypehi:\\n");
    printf("  CSR[c40] = 0x%lx\\n", csr_msettypehi);
    printf("  x5       = 0x%lx\\n", x5_msettypehi);
    printf("After msetsew:\\n");
    printf("  CSR[c40] = 0x%lx\\n", csr_msetsew);
    printf("  x5       = 0x%lx\\n", x5_msetsew);
    printf("After munsetint4:\\n");
    printf("  CSR[c40] = 0x%lx\\n", csr_msetint4);
    printf("  x5       = 0x%lx\\n", x5_msetint4);
    printf("CSR[c41] (mtilem) = 0x%lx\\n", mtilem_init);
    printf("  x5       = 0x%lx\\n", x4_init);
    printf("CSR[c41] (mtilem) = 0x%lx\\n", mtilem_msettilem);
    printf("  x4       = 0x%lx\\n", x4_msettilem);

    return 0;
}
```

编译命令如下：

```bash
clang --target=riscv64-unknown-elf \
  -march=rv64gv0p10zfh0p1 \
  -menable-experimental-extensions \
  -mllvm -riscv-v-vector-bits-min=128 \
  -o mset.riscv mset.c
```

成功生成 `mset.riscv` 文件说明工具链配置正确，可用于后续 gem5 模拟。

- **编译指令可选项详细解析**

  每项作用说明：

  1. -sysroot=$(SYSROOT)

     - 指定交叉编译根目录，头文件/库从该 sysroot 内解析（隔离宿主环境）。

  2. I... (多条)

     - 追加头文件搜索路径：工具链附加 headers、newlib headers、libc++ 头（条件），以及工程自身 include 目录（后面 CXXFLAGS += ...）。

  3. -target=riscv64-unknown-elf

     - 指定目标三元组：64 位 RISC‑V 裸机（无操作系统 ABI）。

  4. march=$(RV_MARCH)

     - 设定具体 ISA 组合/版本（含 G 基础 + V + zfh 等），影响指令可用性与内建。

  5. menable-experimental-extensions

     - 允许使用仍标记为实验状态的扩展（如早期 V/Matrix）。

  6. DPREALLOCATE=1

     - 定义宏 PREALLOCATE=1，触发源码里对应的预分配路径（大缓冲等）。

  7. mno-relax

     - 关闭指令放宽/重写（保留原始 auipc+addi/jal 等形式，地址稳定）。

  8. mcmodel=medany

     - 代码模型：允许访问任意 PC 相对范围内符号（典型裸机中常用），生成 auipc+offset 形式。

  9. static

     - 静态链接，嵌入所有需要的库代码，不依赖运行时动态装载。

  10. std=c++20

      - 使用 C++20 标准。

  11. fno-exceptions

      - 禁用 C++ 异常支持（减小运行库依赖）。

  12. fno-rtti

      - 禁用运行时类型识别（去掉 typeinfo/vtable 相关额外数据）。

  13. fno-common

      - 避免生成 common 符号，未初始化全局直接放进 .bss（防止多重定义隐患）。

  14. fno-builtin-printf

      - 不把 printf 当作内建优化（便于自定义/裸机实现）。

  15. mabi=lp64d

      - 选择 LP64D ABI（64 位长/指针，硬浮点双精度寄存器调用约定）。

  16. D_LIBCPP_HAS_NO_LIBRARY_ALIGNED_ALLOCATION

      -D_LIBCPP_HAS_NO_ALIGNED_ALLOCATION

      -D_LIBCPP_HAS_NO_C11_ALIGNED_ALLOC

  17. 通知 libc++：禁用对齐分配相关接口（目标环境不提供或不想使用）。

  18. D_LIBCPP_HAS_NO_MONOTONIC_CLOCK

      - 告诉 libc++ 没有单调时钟支持，屏蔽相关特性。

  19. D_LIBCPP_ENABLE_ASSERTIONS=0

      - 关闭 libc++ 断言（进一步减小/避免调试开销）。

  20. nostdlib

      - 不自动链接标准启动文件与标准库（自行控制链接输入）。

  21. nostartfiles

      - 不使用默认 crt0/crti/crtn（使用自定义 crt.S）。

  22. nostdinc++

      - 不搜索系统默认 C++ 头（只用显式 -I 的路径，防止宿主污染）。

  (追加) CXXFLAGS += $(includes) -I$(top_dir)/include

  - $(includes) 聚合工程特定的 env/common/src 目录与构建输出目录；再补顶层 include 目录。

------

或是在测试目录下创建 `hellos.c` 文件，并写入如下内容：

```c
#include <stdint.h>
#include <stdio.h>

int main() {
uint64_t csr_init, csr_msettypei, csr_msettype, csr_msettypehi;
uint64_t x5_init, x5_msettypei, x5_msettype, x5_msettypehi;

// 读取执行前的 CSR[0x40] 和 x5
__asm__ volatile("csrr %0, 0xc40" : "=r"(csr_init));
__asm__ volatile("mv %0, x5" : "=r"(x5_init));

// 执行你的自定义指令 (32-bit machine code)
__asm__ volatile("msettypei x5, e16");

// 读取执行后的 CSR[0x40] 和 x5
__asm__ volatile("csrr %0, 0xc40" : "=r"(csr_msettypei));
__asm__ volatile("mv %0, x5" : "=r"(x5_msettypei));

// test msettype
uint32_t csr_temp = 0x12345678;
__asm__ volatile("mv x6, %0" : : "r"(csr_temp));
__asm__ volatile("msettype x5, x6");

__asm__ volatile("csrr %0, 0xc40" : "=r"(csr_msettype));
__asm__ volatile("mv %0, x5" : "=r"(x5_msettype));

// test msettypehi
__asm__ volatile("msettypehi x5, ba");

__asm__ volatile("csrr %0, 0xc40" : "=r"(csr_msettypehi));
__asm__ volatile("mv %0, x5" : "=r"(x5_msettypehi));

// 打印结果
printf("Init:\\n");
printf(" CSR[0x40] = 0x%lx\\n", csr_init);
printf(" x5 = 0x%lx\\n", x5_init);
printf("\\n");

printf("After msettypei:\\n");
printf(" CSR[0x40] = 0x%lx\\n", csr_msettypei);
printf(" x5 = 0x%lx\\n", x5_msettypei);
printf("\\n");

printf("After msettype:\\n");
printf(" CSR[0x40] = 0x%lx\\n", csr_msettype);
printf(" x5 = 0x%lx\\n", x5_msettype);
printf("\\n");

printf("After msettypehi:\\n");
printf(" CSR[0x40] = 0x%lx\\n", csr_msettypehi);
printf(" x5 = 0x%lx\\n", x5_msettypehi);

printf("Hello RISC-V!\\n");

return 0;
}
```

编译命令如下：

```bash
clang --target=riscv64-unknown-elf \\
-march=rv64gv0p10zfh0p1 \\
-menable-experimental-extensions \\
-mllvm -riscv-v-vector-bits-min=128 \\
-o hellos.elf hellos.c
```

再利用 `hellos.elf` 文件生成反汇编 命令如下：

```bash
riscv64-unknown-elf-objdump -d hellos.elf>hellos.dump
```

或者是:

```bash
llvm-objdump -d hellos.elf>hellos.dump
llvm-objdump -d --mattr=+m,+d,+f,+experimental-matrix store.elf>store.dump
llvm-objdump -d --mattr=+m,+d,+f,+experimental-matrix store.elf>./inst/store.dump
```

以上是两种不同的反汇编模式。（反汇编排序不同） 如若在`llvm-objdump`中，想要编译器识别到矩阵指令反汇编，则须在命令中添加`--mattr=+m,+d,+f,+experimental-matrix`以在驱动层调用矩阵指令的反汇编。

最后利用gem5模拟命令如下：

```bash
build/RISCV/gem5.opt --debug-flags=Exec,Registers --debug-file=trace.out configs/deprecated/example/se.py --cpu-type=TimingSimpleCPU --cmd=hellos.elf
```

## **1.5 常见编译安装问题**

> [!TIP]
>
> **问题：**裸机交叉环境缺 C++ 标准库与运行时，导致头文件与符号解析失败
>
> **解决：**单独交叉构建并安装 libc++/libc++abi/libunwind，然后在编译/链接时显式添加它们的 include 与库路径及分组
>
> newlib sysroot（riscv64-unknown-elf），里面没有 libc++ / libc++abi / libunwind。
>
> **问题：**libclang_rt.builtins-riscv64.a 缺失
>
> **解决：**libclang_rt.builtins-riscv64.a 是 LLVM compiler-rt 提供的 “builtins” 运行时库，给编译器生成但目标指令集本身不能直接完成的低级操作提供实现，相当于 GCC 的 libgcc 的替代品（专门版本）。

```bash
CLANG_BUILD=$HOME/opt/riscv/riscv-matrix-project/llvm-project/build-ninja
SYSROOT=$HOME/opt/riscv/riscv64-unknown-elf
mkdir -p $CLANG_BUILD/../build-builtins-rv64
cd $CLANG_BUILD/../build-builtins-rv64

cmake -G Ninja ../compiler-rt \
  -DLLVM_CONFIG_PATH=$CLANG_BUILD/bin/llvm-config \
  -DCMAKE_BUILD_TYPE=Release \
  -DCMAKE_TRY_COMPILE_TARGET_TYPE=STATIC_LIBRARY \
  -DCMAKE_C_COMPILER=$CLANG_BUILD/bin/clang \
  -DCMAKE_ASM_COMPILER=$CLANG_BUILD/bin/clang \
  -DCMAKE_AR=$CLANG_BUILD/bin/llvm-ar \
  -DCMAKE_RANLIB=$CLANG_BUILD/bin/llvm-ranlib \
  -DCMAKE_NM=$CLANG_BUILD/bin/llvm-nm \
  -DCMAKE_LINKER=$(command -v ld.lld) \
  -DCMAKE_C_FLAGS="--target=riscv64-unknown-elf -march=rv64gc -mabi=lp64d" \
  -DCMAKE_ASM_FLAGS="--target=riscv64-unknown-elf -march=rv64gc -mabi=lp64d" \
  -DCMAKE_C_COMPILER_TARGET=riscv64-unknown-elf \
  -DCMAKE_ASM_COMPILER_TARGET=riscv64-unknown-elf \
  -DCMAKE_SYSROOT=$SYSROOT \
  -DCOMPILER_RT_BUILD_BUILTINS=ON \
  -DCOMPILER_RT_DEFAULT_TARGET_ONLY=ON \
  -DCOMPILER_RT_BUILD_SANITIZERS=OFF \
  -DCOMPILER_RT_BUILD_XRAY=OFF \
  -DCOMPILER_RT_BUILD_PROFILE=OFF \
  -DCOMPILER_RT_BUILD_LIBFUZZER=OFF \
  -DCOMPILER_RT_BAREMETAL_BUILD=ON \
  -DLLVM_INCLUDE_TESTS=OFF \
  -DCMAKE_INSTALL_PREFIX=$PWD/install

ninja builtins
ninja clang_rt.builtins-riscv64 
```

然后将 lib/linux/libclang_rt.builtins-riscv64.a复制到之前build好的llvm工具下的lib/linux下

```bash
RES=$($CLANG_BUILD/bin/clang -print-resource-dir) #这里填写自己的路径
cp lib/linux/libclang_rt.builtins-riscv64.a $RES/lib/linux/ 
```



## **1.6 测试负载所需要的python环境**

步骤一：安装系统依赖

```bash
sudo apt update
sudo apt install python3.8 python3.8-venv
```

步骤二：创建并激活虚拟环境（推荐）

```bash
python3 -m venv ~/.venv/riscv-dnn
source ~/.venv/riscv-dnn/bin/activate
pip install --upgrade pip setuptools wheel
#要在当前 shell 使用虚拟环境：
source /home/xl2025/.venv/riscv-dnn/bin/activate
```

步骤三：安装常用包（numpy / pandas / scipy / matplotlib 等）

​	**定义一个requirement.txt文件，包含如下：**

```tex
absl-py==0.10.0
argon2-cffi==20.1.0
astor==0.8.1
attrs==19.3.0
backcall==0.2.0
bleach==3.1.5
cachetools==4.1.1
certifi==2020.6.20
cffi==1.14.2
chardet==3.0.4
cycler==0.10.0
decorator==4.4.2
defusedxml==0.6.0
entrypoints==0.3
google-auth==1.20.1
google-auth-oauthlib==0.4.1
google-pasta==0.2.0
h5py==2.10.0
idna==2.10
importlib-metadata==1.7.0
ipykernel==5.3.4
ipython==7.16.1
ipython-genutils==0.2.0
ipywidgets==7.5.1
jedi==0.17.2
Jinja2==2.11.2
jsonschema==3.2.0
jupyter==1.0.0
jupyter-client==6.1.6
jupyter-console==6.1.0
jupyter-core==4.6.3
Keras-Applications==1.0.8
Keras-Preprocessing==1.1.2
kiwisolver==1.2.0
Markdown==3.2.2
MarkupSafe==1.1.1
matplotlib==3.3.2
mistune==0.8.4
nbconvert==5.6.1
nbformat==5.0.7
notebook==6.1.3
numpy==1.19.2
oauthlib==3.1.0
opt-einsum==3.3.0
packaging==20.4
pandas==1.1.0
pandocfilters==1.4.2
parso==0.7.1
pexpect==4.8.0
pickleshare==0.7.5
Pillow==8.0.0
prometheus-client==0.8.0
prompt-toolkit==3.0.6
protobuf==3.13.0
ptyprocess==0.6.0
pyasn1==0.4.8
pyasn1-modules==0.2.8
pycparser==2.20
Pygments==2.6.1
pyparsing==2.4.7
pyrsistent==0.16.0
PySide2==5.15.0
python-dateutil==2.8.1
pytz==2020.1
pyzmq==19.0.2
qtconsole==4.7.6
QtPy==1.9.0
requests==2.24.0
requests-oauthlib==1.3.0
rsa==4.6
scipy==1.4.1
Send2Trash==1.5.0
shiboken2==5.15.0
six==1.15.0
tensorboard==2.4.0
tensorflow==2.4.0
termcolor==1.1.0
terminado==0.8.3
testpath==0.4.4
tornado==6.0.4
traitlets==4.3.3
urllib3==1.25.10
wcwidth==0.2.5
webencodings==0.5.1
Werkzeug==1.0.1
widgetsnbextension==3.5.1
wrapt==1.12.1
zipp==3.1.0
jax==0.2.17
jaxlib==0.1.69
scikit-image==0.17.2
keras==2.4.0
PyQt5==5.15.6
python-parallelize==1.0.0.0

```

​	建立安装脚本install_requirements.sh，添加可运行权限后对requirement.txt中的包进行安装：

```bash
#!/usr/bin/env bash
set -euo pipefail

# 推荐 Python 版本：3.7/3.8/3.9。若系统没有，请先安装（sudo apt install python3.8 python3.8-venv）。
#PYTHON=${PYTHON:-python3.8}
VENV_DIR=${VENV_DIR:-$HOME/.venv/riscv-dnn}
PYTHON=python3 
REQ_FILE=/mnt/d/riscv-dnn/requirement.txt 
#REQ_FILE=${REQ_FILE:-requirement.txt}

echo "使用 Python: $PYTHON"
"$PYTHON" -V

# 创建并激活虚拟环境
if [ ! -d "$VENV_DIR" ]; then
  echo "创建虚拟环境: $VENV_DIR"
  "$PYTHON" -m venv "$VENV_DIR"
fi

# 激活并升级工具
# 注意：在脚本内激活只对此脚本子shell有效，脚本执行完成后请在交互 shell 中手动 source 激活
source "$VENV_DIR/bin/activate"
pip install --upgrade pip setuptools wheel

# 安装系统依赖（如果需要编译某些包）
# sudo apt update && sudo apt install -y build-essential libssl-dev libffi-dev libblas-dev liblapack-dev

# 使用 requirement.txt 安装
if [ ! -f "$REQ_FILE" ]; then
  echo "未找到 $REQ_FILE，退出" >&2
  exit 1
fi

# 尝试直接安装全部依赖
pip install -r "$REQ_FILE" --no-cache-dir || {
  echo "pip 安装失败，尝试分步安装常见问题包（tensorflow / jax / jaxlib）"
  # 安装 numpy/pandas/scipy 等基础包后再安装大包
  pip install numpy pandas scipy matplotlib --no-cache-dir
  # TensorFlow 2.4.0 对应 Python 3.8 最稳定；若需要 CPU 版本：
  pip install tensorflow==2.4.0 --no-cache-dir || echo "tensorflow 安装失败，请检查 Python 版本或使用官方 wheel 指定"
  # jax/jaxlib CPU 版本（可替换为合适的 wheel URL）
  pip install jax==0.2.17 jaxlib==0.1.69 --no-cache-dir || echo "jax/jaxlib 安装失败，请参照 JAX 官方安装说明"
  # 安装其余依赖
  pip install -r "$REQ_FILE" || true
}

echo "完成。要在当前 shell 使用虚拟环境："
echo "  source $VENV_DIR/bin/activate"
echo "验证： python -c \"import numpy,pandas; import tensorflow as tf; print(numpy.__version__, pandas.__version__, tf.__version__)\""
```

步骤四：验证安装

```python
python - <<'PY'
import numpy as np, pandas as pd
print("numpy", np.__version__)
print("pandas", pd.__version__)
try:
  import jax
  import jax.numpy as jnp
  print("jax", jax.__version__, "backend:", jax.default_backend())
except Exception as e:
  print("jax import failed:", e)
PY
```

